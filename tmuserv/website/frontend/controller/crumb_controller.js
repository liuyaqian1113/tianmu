"use strict"
angular.module(window.ProjectName).controller('crumb_controller', function ($rootScope, $scope, $state) {
    //console.log(crumbService);
    //crumbService.setCrumb($state.current.crumb())
});